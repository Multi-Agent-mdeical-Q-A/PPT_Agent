{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "启动后端",
            "type": "shell",
            // 👇 修改重点：使用 conda run 代替 activate && python
            // -n PPT: 指定环境
            // --no-capture-output: 让日志实时输出
            "command": "conda run --no-capture-output -n PPT python -m uvicorn main:app --reload --host 127.0.0.1 --port 8000",
            "options": {
                "cwd": "${workspaceFolder}/services/backend",
                "shell": {
                    "executable": "cmd.exe",
                    "args": [
                        "/d",
                        "/c"
                    ]
                }
            },
            "presentation": {
                "group": "server",
                "reveal": "always",
                "panel": "new",
                "focus": true
            },
            "problemMatcher": []
        },
        {
            "label": "启动前端",
            "type": "shell",
            "command": "pnpm run dev",
            "options": {
                "cwd": "${workspaceFolder}/apps/desktop"
            },
            "presentation": {
                "group": "client",
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": []
        },
        {
            "label": "🚀 一键启动所有 (All)",
            "dependsOn": [
                "启动后端",
                "启动前端"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}